<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".CoinTrailApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinTrail"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CoinTrail">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- BroadcastReceiver for the daily notification alarm -->
        <receiver
            android:name="com.example.cointrail.notification.NotificationBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- NEW: BroadcastReceiver for notification actions -->
        <receiver
            android:name="com.example.cointrail.notification.NotificationActionReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.cointrail.NO_ACTION_NEEDED" />
                <!-- Add other custom notification action filters here if needed -->
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver to re-schedule alarms after device boot -->
        <receiver
            android:name="com.example.cointrail.notification.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- For some devices, ACTION_LOCKED_BOOT_COMPLETED might be needed too -->
            </intent-filter>
        </receiver>
    </application>

</manifest>